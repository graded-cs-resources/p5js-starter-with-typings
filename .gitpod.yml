tasks:
 - init: npm install && npm install -g browser-sync
   command: browser-sync -f *.html -f *.js --no-open --no-ui --listen 0.0.0.0
ports:
 - port: 3000
   onOpen: open-preview
 - port: 3001
   onOpen: ignore
